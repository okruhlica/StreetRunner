package reverseGeoSuite;
//package reverseGeo

import org.scalatest.FunSuite

import org.junit.runner.RunWith
import org.scalatest.junit.JUnitRunner

@RunWith(classOf[JUnitRunner])
class StreetResolverSuite extends FunSuite {

   test("Passing Test") {
	   assert(true)
   }
   
   test("Failing Test") {
	   assert(false)
   }
}
